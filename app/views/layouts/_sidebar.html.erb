<table class="table">
	<thead>
		<tr>
			<th>ジャンル検索</th>
		</tr>
	</thead>
	<tbody>
		<% genre.each do |genre| %>
		  <tr>
		  	<td><%= link_to genre.name, member_products_path(genre_id: genre) %></td>
		  </tr>
		<% end %>
	</tbody>
</table>